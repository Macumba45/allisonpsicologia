import { FC, memo } from "react";
import FormContact from "../../componets/Formulario";

const Contacto: FC = () => {
  return (
    <>
      <FormContact />
    </>
  );
};

export default memo(Contacto);
